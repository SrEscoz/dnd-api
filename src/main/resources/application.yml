spring:
  profiles.active: dev

  # Configuración de la bb dd
  datasource:
    url:
    username:
    password:
    driver-class-name:
  sql.init.mode: never
  jpa:
    hibernate.ddl-auto: update
    defer-datasource-initialization: false

server:
  port: 8040
  servlet.context-path: /dnd-api

dnd-api:
  api-key: